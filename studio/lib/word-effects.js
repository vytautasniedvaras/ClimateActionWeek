/* ===================================================================
   BYO.WordEffects — word-replacement effects (Scrub + AutoCycle) + a shared
   Perlin noise util + palette helpers. Fleshed out in the effects phase; this
   placeholder defines the namespace so index.html can load it in order.
   Classic-script IIFE on window.BYO — no ES modules.
   =================================================================== */
(function () {
  'use strict';
  window.BYO = window.BYO || {};
  if (BYO.WordEffects) return;
  BYO.WordEffects = {};
})();
